type RequestedCustomerResponse = {
  requested_customers: RequestedCustomer[];
  allowed_requested_customers: RequestedCustomer[];
  requesting_customers: RequestedCustomer[];
};

export default RequestedCustomerResponse;
